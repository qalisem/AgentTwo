# docs/ — Supervisor Source Materials

Drop the two source PDFs Alex sent here (gitignored — kept local only):

- `Gold_Forecast_Spec_v1.pdf` — the original pipeline spec Agent #1
  implemented; Agent #2 follows the same structure with the NVDA signal swap.
- `Prompt_for_Upgrading_AI_Agent_v3.docx` (or PDF) — the adaptive-weighting
  upgrade spec (§3.x) that `lib/adaptive.ts` implements literally.

> **Status:** the PDFs are NOT in this scaffold yet — Qasem to add the
> files Alex provided. Everything in `.gitignore` under `docs/*` is local
> only; only this `docs/README.md` is tracked.
